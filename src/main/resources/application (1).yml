# DO NOT EDIT THIS FILE.
#
# This file is a leftover from a duplicate download (note the "(1)" suffix).
# Spring Boot only loads application.yml / application.yaml / application.properties
# from the classpath, so this file has no runtime effect.
#
# The real configuration lives in: application.yml (same directory).
#
# TODO: delete this file. It is kept around only because the editing session
# could not delete files without user interaction.
